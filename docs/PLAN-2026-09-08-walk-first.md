# PLAN — 會走路優先：離散×連續 2×2 參數實驗波（2026-09-08）

_主人裁示（TG 6014/6022）：「會走路、會看地圖優先」「基於 M0-M2 還有離散和連續位置和動作，
規劃一波參數實驗，medium stitch 跑就好，我要看到螞蟻會走路」。_
_架構裁定（TG 6022、已入 insight）：「離散管動作、連續管座標」混搭可站；路線能否離散＝開放題。_
_TG plan 原文＝6023。環境全部 antmaze-medium-stitch-v0；判準統一：接力成功率＋per-leg 到達率。_

## 2×2 全景

|  | 動作連續 | 動作離散（B2 步法字） |
|---|---|---|
| **位置連續** | 主頭接力 .448 ✅已量（25987） | ← 第一、二波（P1/P2/P4） |
| **位置離散** | P5（M0R 後）| P6 全離散（第三波）|

參考錨：BC 地板 .508／base 分段 .652／lo 頭前任 .288／官方 flat .512。
字典資產：M0 的 L4K32（val MSE .212、perpl 28.6/32、48 gif）＝主格；L8K32 對照。

## 第一波（偵察＋上界；3 隻使魔；主人 go 後發）

- **P1｜M1a 保真重放**：val 軌跡上每 4 步「encoder 壓真動作段→decoder 展開→模擬器執行」，
  量展開軌跡 vs 原軌跡的偏差（xy 終點差、逐步偏差）。獨立 script（experiments/m0_gait_dict/
  延伸）、不動主檔。判讀：偏差小＝壓縮損失不礙走路。
- **P2｜M1b 作弊選字上界**：接力 eval 每 chunk 枚舉 32 字、模擬器前瞻挑「最靠近路標」的字
  →執行。＝「字典＋完美選字」實戰天花板。施工：eval 加 SUB_POLICY=vq_oracle arm（worktree
  ＋golden 流程、旗預設 off）。**判準（預釘）：接力 ≥ .448 → 第二波開；< .448 → 回頭修
  字典（K=64 或 RVQ），不進 M2。**
- **P3｜M0R 路線字典偵察**：計畫層 xy 骨架段（DELTA 尺度）VQ 壓路線字；同 M0 方法論
  （K∈{8,16,32} 使用率／重建／視覺化＝路線字畫在迷宮圖上）。純資料、零主模型。
  回答「路線能不能離散」第一格。

## 第二波（P2 過 .448 才燒）

- **P4｜M2 選字頭**：小 head（同 bc_own 容量紀律、fork_rng 隔離）吃 (s, 路標 xy) → 32-way
  CE，teacher＝hindsight 真字（stage0 encoder 壓）。訓練 100k~200k、接力 eval（splo 的字典版
  SUB_POLICY=vq）。**判準：≥ .448 追平主頭＝字典路線立住；守門 ≥ .288 贏 lo 頭前任。**
  參數格：採樣溫度（argmax vs T=1）一格。
- **P5｜位置離散×動作連續**：conf2 路標 snap 到 M0R 路線字格心（SUB_SNAP 機件的字典版）、
  主頭執行。判準：不輸同 ckpt 連續 conf2 對照。

## 第三波（P4、P5 都站住才有意義）

- **P6｜全離散**：路線字選段＋步法字執行。

## 參數軸（夠用就停、⛔ 不加密）

段長 {4,8} × K=32 主格；K=64 只留作 P2 失敗 fallback；M2 溫度一格。其餘不掃。

## 資源與分工

全 zeldajr（GPU 空）；eval 55 分級、M2 訓練 2hr 級；slurm 排卡鐵則。
第一波分工：P1+P2＝施工使魔一隻（opus，worktree、golden 驗收）；P3＝偵察使魔一隻
（sonnet）；ルナ＝驗收＋判準把關＋向主人報。lo 頭 V 加權工單維持凍結。

## 風險與誠實註記

- M0 單 seed 的保留：P1/P2 天然重驗字典一次。
- P2 的「前瞻」用模擬器＝作弊上界，⛔ 不是可部署方法 —— 它只回答「字典夠不夠好」。
- 抖動尺 ±.03~.07 適用所有判讀；.448 判準線附近的結果要補 seed 再裁。
