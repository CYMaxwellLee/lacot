# FINDINGS — 2026-09-06（大收表判決日）

_⓪ 出處分層：[實測] ルナ親手量　[主人] 主人親口　[讀] 使魔讀回。收表協定＝handoff ②-1 預釘判讀樹＋⑱' 改判版 primary endpoint。分組鍵＝目錄+檔名 `_s`（⛔ json seed 恆 0）。_

## ① 大收表總表 [實測]（R0、`rates.R0`；八顆批 SE 為 seed 間）

```
組（8000 步、八顆）        on      zero    配對差(on−zero)  zero−基線
idp005  (p=.05)          .363    .281    +.083±.016      −.036
idp01   (p=.10)          .335    .269    +.066±.015      −.048
idp8    (p=.30, 7顆)     .326    .310    +.015±.010      −.007
idpxm   (p=.30 @11429)   .431    .403    +.029±.013      +.086 ←唯一轉正
基線 ep200_base .317±.036／f27n(p=0) .454±.040（ep200_f27n .457±.039 錨一致）

單顆格（全 s40 ⚠️ 歷史強 seed）
f27nL  (p=0 @11429)      on .596   zero .000（全崩、見③）
idp01L (p=.1 @11429)     on .596   zero .560
N5L    (無intent @11429)  .304
dvw_pilot (p=.3+L_div)   on .508   zero .488
ws40 續訓（f27n@8000 起 +4000 步）
  ＋idp0.1               on .584   zero .436
  ＋idp0.3               on .556   zero .552 ←配對差 .004
```

## ② idpxm 八顆判決 [實測] — 昨晚煞車完全正確

s40 單顆 on .716 → 八顆 .431±.060。⑱' 的警告（「單顆偷看若成常態會爆表、一切以八顆為準」）逐字命中。s40 強 seed 假象第三次教訓（f27n s40 .656 vs 批 .454 同款）。

## ③ f27nL_zero＝真崩不是 eval 壞 [實測] — dropout 是 zero 能力的必要條件

log 稽核：eval 程序健康（幾何 gate 過、往返尺正常），但五腿全 0/250 — 連只吃 cond 的誠實 BC 地板都 0（on 格 bc=.244）。機制：p=0 模型從未見過 intent 歸零的 cond ⇒ 整個 conditioning OOD ⇒ 全面崩潰。⇒ primary endpoint「zero−f27nL_zero」停用（分母是崩潰態非能力量測）；分母改 N5L。⭐ 與 ⑥ WS 判決互證：同一顆 f27n 續訓注入 p=.3 即救活。

## ④ WS 斜率判準 [實測] — toy「p 驅動衰減」證偽（pre-registered 對決第一格）

divlog log-linear 斜率（丟前 500 步）：idp0.1 −2.112e-4/step、idp0.3 −2.133e-4/step、**比值 0.990** vs 預釘帶 μ₋ 比 2.759±20%＝[2.21, 3.31]（FINDINGS-0905 ⑮ 收緊①）。⇒ 兩臂衰減率相同＝散度衰減是 p 無關的 generic 過程，toy 的動力學形式在此格證偽。（預釘樹兩岔「斜率 0」「比值對上」皆未中 — 落第三態：衰減在、p 不驅動。）

## ⑤ 劑量 Int 四點對決 [實測] — 平衡版 toy 證偽、transient 判別器勝出

Def 1.4（乙檔 v2）、8000 步家族（base=ep200_base .317、ref=f27n .454、分母 .137）：

```
        實測 Int    postA1 pre-registered 預測
p=.05    −.26       +.062 [.020, .167]
p=.10    −.35       +.032 [.010, .091]
p=.30    −.05       +.011
```

四點全打臉 — 8000 步的 zero 全**低於**無 intent 基線＝「殘廢效應」（學了依賴 intent 又被抽走、比從沒學過的更糟）。⑱' 判別器「transient 版由 idpxm 分離」勝出：同 p 多練（idp8→idpxm）zero 超額 −.007→+.086；step-matched 家族（⑱' 裁定新 ref：f27nL/N5L、⚠️ 僅 s40 單顆）s40 單格 Int(idpxm)=1.10、Int(idp01L)=0.88 — 形式上「完美內化」格。⛔ ref 家族單顆、正式讀數等 f27nL/N5L 擴八顆。

## ⑥ WS 續訓判決 [實測] — 「續訓注入 dropout」＝目前最有效的內化療程

s40 單顆：純續訓 zero 全崩（③）；＋p=.1 續 4000 步 zero .436；＋p=.3 續 4000 步 **zero .552 / on .556（配對差 .004）**。代價 on .656→.556（−.10）。⇒ 4000 步的 dropout 續訓把免查模式從崩潰帶到與帶查同分 — 內化 claim 目前最強的單格證據（⚠️ s40 單顆待擴）。

## ⑦ dvw（L_div 結構藥先導）[實測] — 結構在場、單獨不買 R0

散度全程（step 100→8000）warmup 後撐在 ~.34 沒塌（對照 f27n 原生 B/A=1.1% 塌盡）＝hinge 有效。R0 on .508（≈同 seed 劑量臂水平）＝沒跟。預釘樹：「R0 沒跟＝結構藥單獨不夠、不算失敗」。

## ⑧ 步數效應的誠實版 [實測]

批級可說的只有 p=.3 臂 +.105（idp8 .326→idpxm .431、~1.5σ）；s40 同 seed 上 f27n 多練 −.06（.656→.596）、無 intent +.09（.215→.304）、p=.1 +.09（.504→.596）— 單顆全在 2σ 內、混雜。⇒「generic 步數紅利」不成立；步數的作用集中在 dropout 臂＝「慢收斂補課」讀法。⑱' 的「T 效應巨大」在八顆下修成「dropout 臂專屬的慢動力學」。

## ⑨ 大敘事收束

**內化不是平衡態性質、是慢動力學。** 8000 步平衡態處處負 Int（殘廢效應）；11429 步轉正（idpxm zero 超額 +.086、s40 Int~1 級）；續訓 4000 步能救活崩潰的 zero（⑥）。p 的作用是「讓它可能」（③：p=0 則 zero 永不可用）、時間是「讓它發生」（⑤⑧）。toy 的靜態平衡框架輸兩格（④⑤）輸得乾淨 — pre-registered 的價值兌現。藥方改寫方向：dropout 續訓（WS 療程）升主藥候選；退火 p 的理論基礎（toy 動力學）需重建；L_div 保結構配伍用（⑦）。

## ⑩ 待擴清單（呈裁）

f27nL/N5L/idp01L/ws40_idp0.3 各擴八顆（釘 Int 正式讀數＋WS 療程確認）；ws 續訓多步數點（4000→8000）；殘廢效應的機制探針。⚠️ M6 時效件已對帳：idp005 批在跑為真（slurm logs 在）、敘事檔「無臂」句要修。
