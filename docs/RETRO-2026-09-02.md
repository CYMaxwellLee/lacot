# 檢討 2026-09-02 —— 主人 17:39 的兩句話

> 「VQ 故意把 u 限制成有限的字彙 → 這個 codebook size 可以掃，然後去找好一點的 VQ。我記得這邊也很多可以 study，
> VQ 也發展很多年了，也有不同架構和不同訓練方法。你拿什麼 VQ 呢」
> 「妳現在是 Fable 5.1，要避免下結論太武斷，還是得要好好看看問題出在哪」

## 一、我做錯的三件（第一人稱）

1. **拿最陽春的 VQ 就跑、還當成「VQ」的代表。** 我實作的是 2017 年式的單層 VQ-VAE（每 token 一個 code、64 個字、EMA
   codebook、commitment 0.25），加了死 code 重置與加噪。這一代之後的 FSQ、LFQ、RVQ、低維 l2 正規化 code（ViT-VQGAN）、
   rotation trick 我都知道，卻沒有先調研就選了最容易寫的那個；字彙量與層數也只跑一格（64）。
   ⇒ 結果 64 個字一層把路徑資訊砍掉三到五倍（recon 0.044 vs 0.009），然後我差點把「這個陽春版負的」講成「VQ 負的」。

2. **下結論太快，而且每次都太快。** 今天至少三次：
   - 早上把「執行通道不讀 u」當成診斷講給主人（別人的 u = 0.000 其實證明它有在讀；後來 ebfs 證明執行器 1.000）。
   - 「E 看不到口袋」（E 的佔據圖本來就把零資料當牆，它看得到；是路標放錯）。
   - 「VQ 負的」（只是 64 字單層負的）。
   共同形狀：拿一格結果就往「結論」寫，沒有先問「這格能不能區分兩個解釋」。

3. **治療跑在定位前面。** 主人下午說「先把 encoder/decoder 做好、確認 embedding 好」，我把它讀成「重建準」，開了
   長 stage 1 與 VQ 兩帖就灑；結果長 stage 1 把往返誤差全部修到 0.01，成功率卻整個重抽籤（最爛兩顆救活、最好三顆掉），
   相關歸零 ⇒ 表示準了之後失敗全落在 stage 2，而 **flow 那段我到那時為止從來沒有直接量過**，全靠執行結果反推。

## 二、今天站得住的（一手實測、多顆）

- 執行器無罪：路標對 ⇒ 1.000／1.000／0.996／1.000。
- 表示往返精度可修（長 stage 1 ⇒ 0.01），但修好不等於好用（八顆重抽籤、sd 不縮）。
- 定版配方下分級方差主源＝init（D 批 sd .151 vs 資料順序 .056）；舊配方任一隨機源都能引爆災難（C 批）。
- 測試時 E 選計畫救不了（+0.02）；oracle 路徑往返後低分顆仍 0.43~0.66（表示層在特定任務路上歪）。
- 往返尺：最差題 mse 對成功率 r −0.68（V8 八顆），必要不充分。

## 三、還不知道的（⛔ 不准當已知寫）

- flow 對每題 (s,g) 生出的 u 是「整個押錯邊」還是「太散、對的路只佔少數」——**flow 探針**（9/2 晚寫、主人 17:42 點頭）要回答的。
- 哪一種 VQ／哪個字彙量、層數，才能「有錨又不損容量」——要先讀 FSQ／LFQ／RVQ／ViT-VQGAN 的做法再選，不是再猜一格。
- 長 stage 1 讓 u 變細之後，stage 2（flow）是不是也要拉長才公平（LL 臂）。

## 四、改法（規則，不是感想）

1. **先定位、再開藥**：每個「治療」實驗之前，要先有一個能區分至少兩個解釋的探針結果；沒有就先做探針。
2. **一格不下結論**：單一設定（一個 V、一個層數、一個步數）的負結果只能寫「這個設定負」，不能寫「這條路負」。
3. **拿現成方法前先調研它的家族**（半小時就夠）：至少列出三種主流變體、選一種有理由的，把理由寫進設計草稿。
4. **主人說「不懂／太武斷」＝我講太擠或跳太快**，回去拆，不是解釋更多。
