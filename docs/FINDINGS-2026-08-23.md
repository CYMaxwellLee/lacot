# 2026-08-23 — u 診斷、維度、以及 oracle 到底證明了什麼

標記法：`[實測]` 親手量到 ／ `[推論]` 從料推、沒直接驗 ／ `[主人]` 主人裁示

起點是主人的兩個問題：**① oracle 版本是否正確 ② u 沒練好，檢查為什麼**。

---

## 0. 🚨 最重要的結果 — 補上 cond 之後，`u` 沒有貢獻

_⚠️ 本節在 2026-08-23 深夜被自己的後續實驗改寫過一次。先讀這段，再讀 0-b 的舊版。_

`[實測]` K=4 ＋ head 吃 `[cond, u]`，**三個 seed**，官方 eval（MAXH 1000、100 集）：

| | seed0 | seed1 | seed2 |
|---|---|---|---|
| LaCoT R=3 closed | 0.860 | 0.740 | 0.940 |
| LaCoT R=3 open | 0.850 | 0.760 | 0.920 |
| **null-u floor**（`head(cond, 0)`） | **0.850** | **0.770** | **0.930** |
| LaCoT R=0（不 refine） | 0.640 | 0.620 | 0.510 |
| ORACLE closed | 0.600 | 0.700 | 0.710 |
| GCBC floor（⚠️ 容量不公平，見下） | 0.590 | 0.620 | 0.630 |

🚨 **`null-u floor` 跟 LaCoT R=3 幾乎一模一樣** ⇒ **把 `u` 整個歸零，成績不掉。**
⇒ 這一版的 LaCoT 已經退化成一個 GCBC。
⚠️ 精確地說**不是「head 忽略 u」**（那句講太滿，見第 9 節的直接量測）——
是 **head 把對 `u` 的依賴降到一半**，而剩下那點貢獻在 success rate 上顯示不出來。

🚨 而 **`LaCoT R=0` 比 null 還差**（0.51~0.64 vs 0.77~0.93）
⇒ **未經 refine 的 `u` 對 head 是噪聲**；refine 的作用是「把噪聲消掉」，不是「加價值」。

### 四格對照（cond 的有無 × K 的大小）

`[實測]` 補上 K=64 無 cond 之後，圖像完整了：

| | K=4 無 cond | K=64 無 cond | K=4 有 cond（3 seed） |
|---|---|---|---|
| LaCoT R=3 open | 0.700 | **0.800** | 0.850 / 0.760 / 0.920 |
| LaCoT R=3 closed | 0.630 | 0.790 | 0.860 / 0.740 / 0.940 |
| **null-u floor** | **0.000** | **0.000** | **0.850 / 0.770 / 0.930** |
| GCBC floor | 0.410 | 0.680 | 0.590 / 0.620 / 0.630 |
| ORACLE closed | 0.120 | 0.180 | 0.600 / 0.700 / 0.710 |

⇒ **兩個世界，界線非常乾淨：**
- **沒有 cond**：`null-u = 0.000` ⇒ **`u` 是必要的**，而 LaCoT 也確實贏 GCBC
  （0.700 vs 0.410、0.800 vs 0.680）
- **有 cond**：`null-u ≈ LaCoT` ⇒ **`u` 可有可無**

⇒ 也就是說，**「u 有沒有用」完全取決於我們有沒有給 head 另一條路**。這不是 u 好不好的問題，
是**資訊被誰供給**的問題。

⚠️ 順帶一個對「單 seed」的警告：`GCBC floor` 理論上與 `K` 無關（獨立網路、同資料、同 seed），
卻在 K=4 拿 0.410、K=64 拿 0.680 —— 差異只可能來自模型初始化順序（K 改變了前面模組的
參數量，於是 GCBC 的初始化亂數不同）。**同一個 baseline 因為初始化就差 0.27** ⇒ 單 seed
的數字不能拿來下結論，這一晚已經被這件事咬過三次。

### 這是一個設計上的兩難，不是調參問題

- head **沒有** `cond` ⇒ `u` 被迫扛位置資訊 ⇒ 看起來有用（0.700），但餵 OOD 的 u 就崩（ORACLE 0.120）
- head **有** `cond` ⇒ `u` 變成可有可無 ⇒ 方法退化成 GCBC

⇒ **要讓 LaCoT 成立，`u` 必須攜帶 `cond` 沒有的東西。** 候選方向（⏳ 待主人裁示）：
① 讓 `u` 只負責長程意圖，`cond` 只給即時位置；
② 對 `cond` 做 dropout，逼 head 學會用 `u`；
③ 重新設計 `e_target` 的訓練目標，讓它與 `cond` 的資訊不重疊。

### ⚠️ 兩個必須連帶修正的

**(a) GCBC baseline 容量不公平**：腳本裡的 `gcbc_head` 只有 1 個 hidden layer，
而 LaCoT 的 head 有 3 層。⇒ 真正該當 baseline 的是 **`null-u floor`（0.85）**，不是 0.59。

**(b) 真正的天花板已經量到了**（`scratch_calib.py`，同一套 rollout harness）：
```
BFS 專家（尊重牆）    100/100 = 1.000   <- 真正的天花板
直線控制器（忽略牆）    25/100 = 0.250   <- eval 不是靠直線就能過
```
⇒ 這個 eval **可解、且不簡單**。以後對標 **1.000**，⛔ 不要再用那個 OOD 的 ORACLE。

---

## 0-b.〔已被 0 改寫〕舊版：LaCoT 贏 GCBC，open-loop 贏 closed-loop

⚠️ **以下是 head 【未吃 cond】的單 seed 版本。**補上 cond 後 `u` 的貢獻就消失了（見第 0 節）——
這一欄仍有價值：它顯示「當 head 被迫只能靠 `u`」時，refine 過的 `u` 確實能撐起 0.700。

`[實測]` K=4、官方 eval 標準（MAXH 1000、100 集）、head 未吃 cond：

| 變體 | success |
|---|---|
| **LaCoT R=3 open**（想一次、照著走） | **0.700** ★ |
| LaCoT R=3 closed（每 chunk 重想） | 0.630 |
| GCBC floor（無 u） | 0.410 |
| ORACLE closed（每 chunk 重問 BFS） | 0.120 |
| LaCoT R=0（flow 直接採樣，不 refine） | 0.000 |
| null-u floor（`head(0)`） | 0.000 |

**① LaCoT 贏 GCBC 29 個百分點** —— 第一次在最終指標上贏，不是 BC loss。
**② open-loop 贏 closed-loop**（0.700 > 0.630）—— 「想一次然後照著走」贏過「每 4 步重想」。
這正是 LaCoT 的核心主張，而且是量出來的：**想出來的那條路禁得起放著不管**。
**③ refine 是主力不是配角**：R=0 是 0.000、R=3 是 0.63~0.70。

⇒ ⛔ **08-22 的「refine 只是弱的一步改善器、沒有 test-time scaling」在 success rate 上不成立。**
那個結論是用「對齊 e_target 的 cosine」量的 —— 而 refine 做的事根本不是對齊 e_target，
是**把 u 搬到 head 能用的地方**。用錯指標就會得到相反的結論。

### 🚨 而「ORACLE = 天花板」這個框架是錯的

ORACLE 只有 0.120，**低於 LaCoT 的 0.700**。⇒ 它不是上界。

`[推論]`（未驗）原因是 **OOD**：ORACLE 餵的 e_target 來自 `expert_positions()` **模擬**的
BFS 路徑，而 head 訓練時只看過兩種 u —— 資料集軌跡的 e_target、以及 refine 後的 u。
oracle 餵的是**第三種、從沒看過的**分布。

⇒ **ORACLE 是一個 OOD 壓力測試，不是天花板。** 08-22 與本文件先前版本都把它當天花板解讀，
包括「u 還欠 0.51」那種說法 —— **那個數字沒有意義**。

⇒ 若要一個真正的上界，應該餵 head **訓練分布內**的最佳 u（例如資料集裡的真實 hindsight
軌跡），而不是另一個規劃器產生的路徑。

⚠️ **限制**：單一訓練 seed、pilot 規模（6000 步 vs 官方 1,000,000）、K=4 且 head 未吃 cond。
要多 seed 才算數。

## 1. 維度是主因 — flow 一直沒問題

`[實測]` 掃 `K`（PerceiverPooler 的 latent 數，`dim = K × 256`），每格從頭訓一次，
同 cond、同步數，並帶 e_target 健康度與退化控制：

| K | dim | e_target match-acc | flow mode | flow T=1 | regression | 打亂 cond |
|---|---|---|---|---|---|---|
| 64 | 16384 | 0.899 | 0.523 | 0.465 | 0.947 | ~0 |
| 32 | 8192 | 0.937 | 0.540 | 0.466 | 0.943 | ~0 |
| 16 | 4096 | 0.910 | 0.647 | 0.560 | 0.951 | ~0 |
| 8 | 2048 | 0.911 | 0.891 | 0.804 | 0.954 | ~0 |
| **4** | **1024** | **0.911** | **0.934** | **0.877** | **0.954** | ~0 |

**flow 的對齊 0.523 → 0.934，跟 regression 的差距從 0.424 縮到 0.020。**
`match-acc` 全程 0.899–0.937 沒有下滑 ⇒ e_target 沒有被犧牲，這是淨進步。
打亂 cond 全程 ~0 ⇒ 不是灌水。

**為什麼**：`u` 是 `K×D_MODEL` 維，而它描述的軌跡只有 `T_CAP × 2 = 32` 維真實內容。
K=64 時是 **512 倍的膨脹**。regression 對此無感（只是參數多），flow 要在整個空間建密度，
被維度直接壓死。

⇒ `[實測]` **「flow 的 mode 放錯位置」這個結論是錯的** —— mode 一直放得對，是 16384 維的
空間讓它採不到。本文件先前版本的那個判斷已作廢。

⇒ 這也讓「flow vs regression」的對立消失：K=4 時兩者準度相當，而 flow 還多帶著 exact
likelihood（能評估想法好壞）、多候選採樣（test-time scaling 的入口）、可微密度
（value-directed refine 要用的地形）。

### 沿路排除掉的兩個假設

`[實測]` **尺度不匹配 → 排除**：`||true_et||` = 129.7 ± 2.6，flow base 期望 `√16384` = 128.0，
比值 1.013。

`[實測]` **高維採樣散開 → 有，但只佔一小段**：溫度 1.0 → 0.0 只把對齊從 0.468 拉到 0.523
就 plateau。真正的差距不在採樣。

`[實測]` **p(e|cond) 很窄**：(s,g) 幾乎相同的 135 組，e_target 互相的 cosine 0.840
（隨機配對基準 0.022）。在 navigate 上路徑接近唯一。
⚠️ `[推論]` 早先由此推出「stitch 有多模態所以 flow 有用」——**未經驗證**，而且查過
OGBench 之後不確定成立：navigate 與 stitch 是**同一個 env、同一組 task goal**，只差在
資料收集方式（`gymnasium` 註冊表裡沒有 `-navigate-`/`-stitch-` 的 env）。stitch 的困難
`[推論]` 是「訓練時看不到長距離 (s,g)、要靠組合」，這跟多模態是不同的東西。**待驗**。

---

## 2. Oracle 證明的比宣稱的少

`[實測]` 讀 `scratch_m4_oracle.py` 得到三件事：

**① 它是 closed-loop。** `rollout` 每個 chunk（4 步）都用當下的 obs 重新呼叫
`env.unwrapped.get_oracle_subgoal()` —— 環境內建的 BFS。所以不是「想一條路照著走」，
是每 4 步問一次導航。head 只需要翻譯剛算好的路徑前幾個點。

**② e_target 的來源與訓練時不同。** 訓練用資料集的真實 observations；oracle 用
`expert_positions()` 自己模擬的簡化點動力學（`xy += 0.2*a`，沒有 MuJoCo 慣性）。
餵 OOD 的東西也能 100% ⇒ e_target 空間很寬容。

**③ 訓練與 eval 的 goal 距離差一個量級。** 訓練 `geometric(p=0.02)` 平均 ~50 步；
eval 實測 start-dist 27.9、每步最多 0.2 ⇒ 需要 140+ 步。而 `T_CAP=16` 個取樣點要
覆蓋的長度差了近 3 倍。

**④ 〔本節初版寫錯，已更正〕** 初版寫「`scratch_m4_oracle.py` 用 `ActionMLP` 取代
`DiscretizedActionHead`，所以那天的數字是**替身**的」——**這個判斷是錯的**。
`[主人]` 那不是疏失，是 08-22 當天發現離散 head 不行之後**刻意改成 MLP** 的。

`[實測]` 而今天的 head 診斷等於把那個決定重新證明了一次（同一個真 e_target、同樣 5000 步，
共同指標＝decode 回連續 action 的 MSE）：

| head | MSE @5k |
|---|---|
| MLP + MSE（08-22 改用的） | **0.375** |
| Discretized 32 bins ＋同樣 3 層 MLP | 0.845 |
| Discretized 256 bins ＋同樣 3 層 MLP | 0.903 |
| Discretized 256 bins（本體原樣，單層 proj） | 1.286 |
| 猜資料集平均（什麼都不學） | 0.494 |

⇒ 補到同容量之後，離散 head 仍然**比什麼都不學還差**。cross-entropy 對「落在隔壁 bin」
不給任何分數，而那正是連續控制需要的訊號。bin 數有影響但很小（32 每格都優於 256，
`[主人]` 昨天講的方向成立），不是主因。

⇒ **行動**：把 MLP head 正式寫進本體（`lacot/heads.py::ContinuousActionHead`），
`LaCoTActorState` 預設改用它，`DiscretizedActionHead` 加上「連續控制別用」的警告。
本體與實際做法脫節，正是這次誤判的成因。

⇒ 正確讀法：那個 100% 是「**closed-loop BFS ＋ MLP head 的翻譯能力上限**」，
不是 LaCoT 這個方法的上限。「架構證明完美」的說法作廢 —— 但理由是 ①②③（closed-loop、
e_target 來源、goal 距離），**不是** head 的問題。

### 修正：closed-loop vs open-loop

- **closed-loop**：每個 chunk 重算 u。錯誤不累積。跟官方比較要用這個（官方 GCBC 甚至每步重決策）。
- **open-loop**：u 只在起點算一次，全程沿用；cond（自己在哪）仍每 chunk 更新。
  凍住的是「想出來的那條路」。

⇒ `[推論]` **兩者的差距本身就是 reasoning quality 的直接量測**：差距小 = 想出來的路
禁得起放著不管。LaCoT 若要主張「會推理」，證據在 open-loop 那一欄。

---

## 3. 合規性：對照官方 `impls/`

`[實測]` 逐項比對 `impls/utils/evaluation.py`、`impls/main.py`、`impls/agents/gcbc.py`：

| 項目 | 官方 | 我們（08-22） | 判定 |
|---|---|---|---|
| eval env | `pointmaze-medium-v0` | 同 | ✅ |
| success 語義 | `_terminate_at_goal=True`，到目標即 terminate | success 即停 | ✅ 等價 |
| action clip | `[-1,1]` | 同 | ✅ |
| max horizon | **1000** | **500** | ❌ 已修 |
| eval episodes | 20 × 5 tasks = 100 | 6 × 5 = 30 | ❌ 已修 |
| 決策頻率 | 每步 | chunk 4 步 | ⚠️ 方法自己的，須標明 |
| relabel | `actor_p_trajgoal=1.0`, `geom_sample=False`（均勻） | `geometric(p=0.02)` | ⚠️ 方法自己的（`[主人]` 合規），但**這正是訓練/eval 距離不匹配的來源** |
| batch size | 1024 | 64 | 🚨 16× |
| train steps | **1,000,000** | **3,500** | 🚨 285× |

⇒ `[實測]` 總 compute 差約 **4500 倍**。今晚所有數字都是 pilot 規模，**不能與官方
GCBC 的 99% 並排**。而且 flow 的 `l_nf/dim` 在 2000 步時仍在下降（-1.91 → -2.70），
⇒ 「flow 輸給 regression」有可能只是**在對 flow 不利的時間點量的**（`exp_u_steps` 正在驗）。

**已修**：四支腳本的 `MAXH` 改為 env 官方 horizon、每 task 20 集，可用
`LACOT_EVAL_MAXH` / `LACOT_EVAL_EPISODES` 覆蓋。

---

## 3.5 交接的真正缺口：記了「做了什麼」，沒記「為什麼」

`[實測]` 今晚同一個形狀的誤判發生三次：08-22 的 handoff 記下了結果（改用 MLP、
oracle 100%、bin 數），但沒有記**決策的理由**。於是今天讀到 code 與結果卻不知道原因，
就自己補了一個解釋 —— 而且方向補錯（把「刻意的修正」讀成「偷懶的替身」）。

⇒ 交接檔要有「**決策與理由**」區，專門放「為什麼不是另一種做法」。結果在 code 裡看得到，
理由看不到。

---

## 4. 新增：`LaCoTActorState`

`[實測]` `LaCoTActor` 的介面是**影像**的（`s_frame [B,3,H,W]`、`future_frames [B,T,3,H,W]`、
`generator.encoder`）—— 這才是實驗腳本自己寫 head 的原因，不是偷懶。

新增 `lacot/model.py::LaCoTActorState`：換掉前端為低維狀態編碼器，**保留真正的
Flow / RefineOperator / DiscretizedActionHead** 與同樣的三個 loss。13.9M 參數，
已驗 forward 與 action 解碼範圍。

---

## 5. 🚨 維度的權衡（⚠️ 前提後來被推翻了一半，先讀這段）

⚠️ **下面這組數字是在【action head 只吃 `u`、沒吃 `cond`】的前提下量的。**
⚠️⚠️ 而且ルナ一度把 ORACLE 的 0.120 歸因於「head 沒吃 cond ⇒ 閉著眼睛開車」——
**那個解釋不成立**：同一版 head 也沒吃 cond，LaCoT 卻拿到 0.700。閉著眼睛開不出 0.700。
真正的嫌疑是 OOD（見第 0 節）。cond 該補仍然該補（多給資訊不會壞），但它不是 0.120 的成因。
`[實測]` 後來發現本體的 head 只餵 `u`（08-22 的腳本是餵 `[cond, u]`），而這個差異讓
K=4 的 ORACLE 從 100% 掉到 **12/100**。⇒ K 小的劣勢有一大部分其實是「位置資訊被壓掉、
而 head 沒有 cond 可問」造成的，不是維度本身。**cond 補回之後要重跑，⛔ 別直接沿用下表。**

（已修：head 輸入改 `cond_dim + k*d_model`；驗證方式＝同一個 `u` 餵不同 `cond`，
動作差異 0.5599 ⇒ cond 確實進到動作裡。）


`[實測]` 同一支 head 診斷在 K=64 上再跑一次（其他不變）：

```
                  K=4 (1024維)   K=64 (16384維)
flow 對齊            0.934          0.523      ← 降維救了 flow
action head MSE      0.375          0.278      ← 降維【傷了】head（差 35%）
```

`u` 的維度同時是兩件事 —— flow 要建模的空間**大小**（要小才學得動）與 action head 能讀到的
資訊**容量**（要大才預測得準）。**兩個需求方向相反**，而我們一直用同一個數字同時壓它們。

⇒ 這回頭解釋了 08-22：K=64 時 oracle 能到 100%（head 資訊夠）但 LaCoT 本身很低
（flow 生不出好 u）。那個落差不是「flow 不行」，是**同一個維度綁死了兩個對立的需求**。

`[推論]` 候選解法：**解耦**。flow 在低維生 `z`，再用 decoder 展開成高維 `u` 餵 head：
```
cond ──flow──► z（低維，好採樣） ──decoder──► u（高維，資訊足） ──► action
```
「想」在低維、「執行」在高維 —— 也更貼近 CoT 的原意：念頭本來就是壓縮的，展開是後面的事。
⏳ 等主人判斷方向。

## 6. 訓練訊號的觀察（Continuous head，K=4，6000 步）

```
基準（猜資料集平均）             0.4940
l_act_anchor（吃【真】e_target）  0.3264   比基準好 34%
l_null（沒有 u）                 0.4765   ≈ 基準（合理：沒 u 只能猜平均）
l_act_refine（flow + refine）    0.2774
l_gcbc（直接 s,g → action）      0.2787
```

⚠️ 兩個要追的：
1. **GCBC(0.279) 比吃真 e_target 的 anchor(0.326) 還準** ⇒ 在 BC 準度這件事上，目前
   「經過 u」是淨損失。⛔ 但這是 BC 指標不是 success rate，不足以下結論。
2. **`l_act_refine`(0.277) 優於 `l_act_anchor`(0.326)**，而 anchor 吃的是真值 —— 反直覺，未解。

⚠️ `[修正]` 早先看到 `exp_u_steps` 在 20000 步掉到 0.438 就說「訓練越久越差」，
但 35000 步回到 0.499、50000 步 0.474 ⇒ **那是震盪不是下降**，是拿單點定趨勢。
正確說法：**K=64 時 flow mode plateau 在 ~0.47 上不去**（regression 已 0.965）。
另一個真的在單調變化的是 `flow_T1`：0.423 → 0.352 → 0.326 → 0.268 ⇒ 訓練越久，
分布越尖，而 T=1 採樣仍落在殼上 ⇒ 離 mode 越來越遠。⇒ 實務上該用低溫採樣。

## 進行中

- `exp_oracle_true.py` — Continuous head ＋ 官方 eval ＋ closed/open 七對照，K=4 與 K=64 各一份
- `exp_u_steps.py` — 1k→100k 步的對齊曲線（K=64），50000 步時 flow mode 0.474 / regression 0.965

## 6.5 🚨 cond 的效果（三個 seed）＋ 一個很強的對比

`[實測]` head 補上 `cond` 之後，K=4 三個 seed：

| | ORACLE closed | ORACLE open |
|---|---|---|
| 無 cond（單 seed） | 0.120 | 0.010 |
| 有 cond seed 0 | **0.600** | 0.000 |
| 有 cond seed 1 | **0.700** | 0.000 |
| 有 cond seed 2 | **0.710** | 0.000 |

⇒ **cond 讓 ORACLE closed 從 0.12 跳到 0.60~0.71（+0.5）。**

⚠️ **ルナ在這裡過度否定了自己一個正確的診斷。** 先是判斷「head 沒吃 cond 是 0.120 的主因」
（對的），接著因為「同一版 head 也沒吃 cond，LaCoT 卻有 0.700」就整個推翻它，改口說主因是
OOD。**那個反駁是錯的** —— 兩個因素是**相乘**的：
- head 沒有 `cond` ⇒ 位置資訊只能從 `u` 讀
- ORACLE 的 `u` 又剛好是 OOD ⇒ 讀不出來 ⇒ 崩到 0.120
- 而 LaCoT 的 `u` 是 in-distribution ⇒ 讀得出來 ⇒ 就算沒有 cond 也有 0.700

⇒ 補上 `cond` 等於給 head 一個**獨立於 `u`** 的位置來源，OOD 的傷害就被吸收掉了。
⛔ 教訓：**一個反例只能否定「唯一主因」，不能否定「是主因之一」。**

### 🚨 而 ORACLE open 三個 seed 全是 0.000 —— 對比極強

```
BFS 算出的「客觀正確路徑」，放著不管走完全程   ->  0.000
LaCoT 自己想出來的 u，放著不管走完全程        ->  0.700（無 cond 版實測）
```

`[推論]` ORACLE 的 `e_target` 編的是**從起點出發的絕對座標軌跡** —— agent 一走，那條路
就過期了。而 LaCoT 的 `u` 經過 refine，看起來編的是**更適合持續執行的東西**（可能偏向
相對方向或策略，而非絕對路徑）。

⇒ 如果 LaCoT 的 open-loop 優勢在多 seed 下站得住，**這會是「LaCoT 的 u 不是路徑、
而是更好用的東西」的直接證據** —— 那正是 latent CoT 該有的樣子。⏳ 等三個 seed 的 LaCoT 欄。

---

## 7. ⚠️ 一個解讀上的變化：`null-u floor` 不再是「地板」

`[實測]` head 改吃 `[cond, u]` 之後，`head(cond, 0)` 就等於「從 (s,g) 直接預測 action」
—— 也就是一個 GCBC。訓練 3000 步的實測：

```
無 cond：null 0.4597  gcbc 0.2408   <- null 接近「猜平均」(0.494)，是真地板
有 cond：null 0.2336  gcbc 0.2356   <- 幾乎一模一樣 => null 就是 GCBC
```

⇒ ⛔ 不要再把 `null-u floor` 讀成「什麼都不學的地板」。它現在是 **BC baseline**。
⇒ 這其實更有用：**`anchor` 或 `refine` 減掉 `null`，直接就是「u 帶來多少 GCBC 以外的價值」**。
⇒ 也代表舊版那個 `null-u floor = 0.000` 的 rollout 數字**不能跟新版並排**。

（順帶 `[實測]`：三個 seed 在 3000 步的 anchor 是 0.199 / 0.218 / 0.290 —— seed 之間
差距不小，證實了單一 seed 不足以下結論。）

---

## 9. `[實測]` 直接量 head 對 `u` 的依賴（不用從結果反推）

`exp_u_sensitivity.py`：同一批資料、同一個 `u`，並排訓一個吃 `[cond,u]` 的 head 和一個
只吃 `u` 的 head（對照組），再量三件事。

**梯度範數（每維度）**
```
有 cond：  ||∂a/∂u||    7.96e-03
          ||∂a/∂cond||  1.63e-02     <- cond 的影響力是 u 的 2 倍
只吃 u：   ||∂a/∂u||    1.73e-02
⇒ 有 cond 時 head 對 u 的梯度只剩 46%
```

**歸零測試（MSE 對真動作）**
```
都不動           0.252
u 歸零           0.327   <- 變差 30% => u 確實【有】貢獻
cond 歸零        0.781   <- 變差 3 倍 => cond 重要得多
只吃 u 的 head    0.359   <- ⚠️ 比「u 歸零」還差
```

🚨 **最後一行是關鍵：只有 `cond`（0.327）比只有 `u`（0.359）還準。**
⇒ 對這個任務，**`cond` 攜帶的資訊量本來就大於 `u`**。head 不是壞掉，是理性地把權重
放在資訊多的一邊。

⚠️ **而且這裡有個必須記住的矛盾**：`u` 歸零讓 MSE 變差 30%，但 **success rate 完全沒掉**。
⇒ **action MSE 不是 success rate 的好代理指標。** 今晚被這件事誤導過兩次
（另一次是 refine：用「對齊 e_target 的 cosine」量，得到跟 success rate 相反的結論）。
⛔ 以後任何關於方法好壞的判斷，都要回到 success rate。

⇒ **問題不是「u 沒資訊」，是「cond 太好用，u 的那點資訊被蓋過去」。**
`[推論]` 最直接的解法是**對 `cond` 做 dropout**：訓練時隨機遮掉 cond，逼 head 學會從 `u`
讀出位置與規劃；推論時再把 cond 給回去。⏳ 待主人裁示。

---

## 8. ⏳ 未解：官方 GCBC 跑不出論文的 99%

`[實測]` 用 `hyperparameters.sh` 指定的指令（除 `eval_episodes` 20 vs 50 外完全一致）
跑滿 1,000,000 步，overall success 只有 **0.15**（best 0.18）；論文 Table 2 是 **99±6**
（已用一手來源確認，不是 08-22 的誤讀）。訓練 MSE 只從 0.421 降到 0.362，而「猜資料集
平均」是 0.494 —— 也就是只學到 27%。

`[實測]` 已排除的：
- **資料集與 env 匹配**：把 env 用資料集的 qpos/qvel 設回原狀態、餵記錄的 action，
  50 步累積誤差 **0.0000**；負控制（打亂 action）拉開到 0.328。
- **資料內容與官方 loader 一致**：逐元素最大差 0.0（長度差 1001000 vs 1000000 是
  loader 移除每軌跡最後一個沒有 next state 的 transition，合理）。
- **eval 可解且不簡單**：BFS 專家 1.000、直線控制器 0.250。
- **指令與官方一致**：`hyperparameters.sh` 第 2 行就是純預設值。

`[推論]` 還沒驗的嫌疑：
1. **pointmaze 的 observation 只有 (x, y)、沒有速度**，而 point 有慣性 ⇒ 對 Markovian
   policy 而言這是 POMDP。官方每步重新決策；我們是 chunk 4 步（等於時間平滑），
   在慣性系統上可能反而穩定 —— 這也許正是我們的 GCBC(0.59~0.63) 高於官方(0.15) 的原因。
2. **action 分布是雙峰的**（`|a|>0.99` 佔 28%）⇒ 用高斯 actor ＋ `eval_temperature=0`
   取 mean，可能落在兩峰中間、幅度太小而推不動。
3. JAX 0.10.2 / flax 0.12.8 相對 repo 當時的版本過新。

⛔ **在這件事釐清之前，不要拿我們的數字跟論文的 99 並排。**

---

## 待辦

0. **多 seed 驗證 0.700**（已加 `LACOT_SEED`，env seed 刻意固定讓各 seed 跑同一組 100 集）。
   單一 seed 的 0.700 不足以下結論。
0b. **設計一個真正的上界**取代現在的 ORACLE：餵 head **訓練分布內**的最佳 u
   —— 例如直接用資料集裡的 hindsight 真軌跡編出的 e_target，而不是另一個規劃器
   （`expert_positions` 的模擬 BFS）產生的路徑。現在這個 ORACLE 只能當 OOD 壓力測試。
0c. **重測 open vs closed 的差距**在多 seed 下是否穩定 —— 那個差距是 LaCoT
   「會推理」的直接證據，值得單獨、嚴謹地量。
1. stitch 資料**手上沒有**（只有 pointmaze medium/large 的 navigate）。要驗多模態假設得先下載。
2. relabel 從 `geometric(0.02)` 改成官方那種均勻 trajgoal，看訓練/eval 距離不匹配是否改善。
3. 自己實作段邊界偵測（oracle 曾用 fpo 的 `derive_subtrajectory_boundaries`）。
4. value-directed refine（value 已訓好在 `checkpoints/scratch_value.pt`）。

---

## token head 的 2^3 全因子 ablation（主人 2026-08-23 指定「不要只單純全部加」）

`[實測]` 每格 3 個 seed，`exp_three_fronts.py`，`MODE=tok_*`。
增益＝`1 - MSE(餵真 e_target) / MSE(u 餵零)`，量的是 **head 有多依賴 u**。

```
           readout wide uproj    增益        參數
tok_000       ·    ·    ·     +3.4% ±1.6   1.6M
tok_r00       ✔    ·    ·     +4.2% ±1.4   1.8M
tok_0w0       ·    ✔    ·     +2.1% ±1.7   6.4M
tok_00u       ·    ·    ✔     +2.5% ±0.8   1.7M
tok_rw0       ✔    ✔    ·     +3.1% ±1.8   7.2M
tok_r0u       ✔    ·    ✔     +4.8% ±2.5   1.9M
tok_0wu       ·    ✔    ✔     +0.9% ±0.7   6.6M
tok_rwu       ✔    ✔    ✔     +2.5% ±1.2   7.4M
concat 對照   —    —    —     +9.7% ±1.9   1.2M

主效果（開的四格 減 關的四格，每邊 12 次，隨機誤差 ±0.8 pt）
  readout  +1.4 pt      wide  -1.6 pt      uproj  -0.6 pt
```

★ **主人要求 ablation 擋掉了一個誤判。** 原訂做法是三個一起加＝`tok_rwu` 的 +2.5%，
**比什麼都不加的 +3.4% 還低**。只跑那一格會讀成「token 結構沒救」，
實際上是 `wide` 在拖（主效果 −1.6 pt），而 `readout` 是有幫助的（+1.4 pt）。

⚠️ 設計上的一個坑：`wide` 一開，u 的 256 維對不上內部的 512 維，一定得有東西把它抬上去
⇒ `wide` 會自動夾帶 `u_proj`，兩格永遠分不開。所以 `u_proj` 關掉時走**零填補**
（不含參數、不學習）。`tok_0w0`（零填補）和 `tok_0wu`（真投影）都輸給基準，
⇒ 「wide 有害」不是零填補這個對照造成的。

## 🚨 更大的不公平：兩邊根本不是同一種量法

`[實測]` 讀 `run_head`（concat）與 `run_head_token` 的訓練迴圈才發現：

```
             u 存在時用哪顆 head     只cond 那個數字哪來的
concat 版      head_u（專用）         head_c，另一顆獨立的 net
token 版       head_u（共用）         同一顆 head_u，把 u 餵 0
```

concat 的 `head_u` **從頭到尾沒看過 u 缺席**，可以放心全靠 u；
token 那顆要同時服務「u 在」與「u 是 0」，一顆網路兩種輸入都要交代得過去。
共用等於一個很強的壓力在逼它**別依賴 u** —— 而增益量的正好是「有多依賴 u」。

⇒ **這三張表上的 `concat +9.7%` vs `token +4.8%` 不能直接讀成「架構差別」。**
⇒ 已加 `MODE=pr_*` 六格（架構 × 量法 2x2，token 取基準與 r0u 兩種設定），主人核可。

⛔ 同一個病也在 `run_cond_size`：它全程用 token＋共用協定，所以 cond 大小那個
比較**內部一致、結論仍成立**（cond 小 ⇒ 增益高），但它的絕對數字不能拿去跟 concat 比。

### 協定對齊 2x2 的結果：⛔ 假設被推翻，協定不是原因

`[實測]` `MODE=pr_*`，六格 × 3 seed：

```
             專用 A對B      共用 C自己比
concat      +8.0% ±2.3     +7.7% ±2.1
token 基準   +4.6% ±1.4     +3.6% ±1.8
token r0u   +4.4% ±2.3     +4.6% ±2.2
```

⛔ **上面那個「共用協定在壓 token 版」的推論是錯的。** 兩欄幾乎一樣：
concat 換成共用還是 7.7、token 換成專用還是 4.6。協定差別在數字上不重要。

⇒ **concat 贏是真的**（四組對照同向，差 3~4 pt），不是量法佔便宜。
★ 這條留著當教訓：那個「一顆網路要同時服務兩種輸入 ⇒ 會學會不依賴 u」的論證
  聽起來完全成立，也確實描述了訓練壓力 —— 但**壓力存在不等於它是主導項**。
  ⛔ 講得通的機制 ≠ 量到的原因。

⏳ 剩下的主嫌：**B1（動作從哪顆 token 讀出）**。token 版至今仍是只讀 cond 那顆，
u 得先靠 attention 搬進去才影響得到輸出；concat 是 1280 維直接接到輸出、沒有這道關卡。
`readout_mode`（cond / pool / query）已在 `TokenActionHead` 實作好，待主人點頭再跑。

### 為什麼 concat MLP 會贏 self-attention（主人問：「transformer 自己的 FFN 也是 MLP 呀」）

`[推論]` 答案就在那句話裡：**FFN 是逐 token 各做各的**，它不負責混合 ——
一顆 token 的 FFN 只看得到自己那 256 維。跨 token 的混合**只能靠 attention**，
而 attention 是「加權平均」：softmax 權重非負、和為一。

concat 的 MLP 第一層是 `Linear(1280 -> 512)`，那 512 個輸出**每一個都是全部
1280 維的自由線性組合**，正負皆可、每一維各有各的權重。

具體差在哪：假設正確動作跟 `u1 - u4` 有關。
- MLP：第一層權重寫成 `+1` / `-1`，一步到位。
- transformer：加權平均出不了減法 ⇒ 要兩顆 head 各自把 u1、u4 挑出來（attention 要
  學到近乎 one-hot），再由 `W_o` 相減。做得到，但繞一大圈、要學。

更根本的一項：**transformer 對每顆 token 共用同一組權重**（`W_v` 對 u1~u4 是同一份），
concat MLP 則是 u1 那段跟 u4 那段各有各的權重。共用在 token 多的時候是優點
（可以互相 generalize），但這裡 **K=4**，沒什麼好 generalize 的 ⇒ 共用只剩損失。

⇒ 不是 transformer 爛，是**它的優點在這個尺度還沒生效、缺點先付了**。

⏳ 由此衍生的候選（待主人裁示）：**MLP-Mixer**。Chain-of-Goals（arXiv 2602.03389）
用的其實是 Mixer 不是 self-attention —— Mixer 的跨 token 混合是一層普通線性層，
可自由加減、不受加權平均限制，剛好卡在 concat MLP 與 transformer 中間。

### B1（動作從哪顆 token 讀出）：⛔ 也被推翻

`[實測]` `MODE=b1_*`，協定固定 ded，每格 3 seed：

```
                   增益      真et    只cond
讀 cond 顆（現況） +4.8% ±1.4  0.2533  0.2662
五顆平均 pool     +5.1% ±2.3  0.2517  0.2650
專用 query token  +3.6% ±1.5  0.2572  0.2669
concat 對照       +8.0% ±2.3  0.2451  0.2662
```

pool 比現況只高 0.3 pt（誤差 2.3），query 更差 ⇒ **讀出位置不是主因**。
⚠️ 只cond 那欄四格幾乎一樣（0.265~0.267），⇒ 差別確實出在「u 在場時用得多好」，
不是基準漂移。concat 的真et 0.2451 是四格最低。

★ 過程教訓：中途只有 1 個 seed 時 pool 是 **+6.4%**，看起來像找到了；
3 個 seed 收齊掉回 +5.1% ±2.3。⛔ 別在 seed 沒齊時回報方向。

⇒ 連兩個假設（協定、讀出位置）被推翻後，剩下的解釋回到機制本身：
**attention 的跨 token 混合是加權平均，做不出自由加減** —— 換讀出口修不掉，要換混合方式。
⏳ 下一步：`MODE=mx_*`（MixerActionHead，跨 token 改普通線性層），已實作、待主人點頭。

### mixer：⛔ 第三個假設也被推翻，但排出了一條有意義的線

`[實測]` `MODE=mx_*`，協定 ded，每格 3 seed：

```
                     增益      真et    參數
concat MLP        +8.0% ±2.3  0.2451  1.19M
attention 平均     +5.1% ±2.3  0.2517  1.65M
attention 讀cond   +4.8% ±1.4  0.2533  1.65M
mixer 平均        +2.1% ±1.9  0.2573  1.12M
mixer 讀cond      +1.5% ±2.2  0.2611  1.12M
```

⛔ 預測是「mixer 的跨 token 混合是自由線性層、做得出減法 ⇒ 應該贏 attention」。**輸了。**

★ 但三個架構排出一條線，而且順序＝**共用權重的程度**：
- concat：完全不共用，每一對維度各有自己的權重
- attention：value 投影對每顆 token 共用，但有多顆 head、且權重看資料決定
- mixer：跨 token 那層對**全部 256 個 channel 共用同一張 N×N 混合表**，最死

⇒ 一致的解釋：**K=4，沒有東西好共用**。共用權重的價值來自「很多同類的東西可以互相
generalize」，只有四顆時任何共用都只是丟容量。concat 不是特別聰明，是**沒有在這個
尺度上付不該付的代價**。

⚠️ 保留：mixer 的 token-mixing 隱藏寬度設成 `4*n_tok = 20`，偏瘦 ⇒ 「mixer 最差」
有一部分可能是那層太小，不全是結構。要完全排除得加寬再跑。

⇒ **架構線收在 concat。** ★ 而主人指出的下一個缺口更值錢：concat 這幾格一直背著
`COND=256`（cond sweep 量到最差的那個設定）在跑，⏳ 待跑 `concat + cond 64`。
⚠️ 標注：「256 最差」是拿 **token head** 量的，⛔ 還沒驗過在 concat 上是否成立。

### 三個假設連續被推翻的共同教訓

協定 → 讀出位置 → 混合方式，三個都是**機制上講得通、也確實存在**的效應，
量出來卻都不是主導項。★ 有效的反而是最後那個「回頭看三者的共同軸」——
⛔ 不是再多想一個新機制，是把已經量到的東西排成序列去找規律。

### 🚨 修正：「cond 64 最好、256 最差」是 1 個 seed 的運氣，不成立

`[實測]` `MODE=cc*` / `tc*`，全部 3 seed、ded 協定、同一套自建 cond 編碼器：

```
 cond |  concat 增益   concat 真et |  token 增益   token 真et
  256 |  +8.9% ±1.6      0.2421    |  +5.9% ±3.9     0.2519
   64 |  +7.8% ±2.6      0.2464    |  +6.2% ±2.1     0.2532
   16 |  +8.0% ±2.1      0.2471    |  +6.0% ±2.5     0.2543
    4 |  +8.2% ±1.5      0.2536    |  +7.5% ±1.4     0.2579
```

⛔ **舊結論作廢**：`cond64 +11.4% vs cond256 +6.0%`（來自 `fronts_cond*_s0`，**只有 1 seed**）
三個 seed 重跑後整條線變平，token 四格全在 6% 附近，256 沒有比較差。

★ 特別記這個：當時ルナ用「真et 和只cond **兩欄同向**」佐證它不是分母造假。
那個檢查邏輯正確，⛔ **但擋不住 1 seed** —— 兩欄可以一起走運。
⇒ 同向檢查是【必要】不是【充分】，前提仍然是 seed 要夠。

⇒ **head 這條線收掉**：`concat + cond 256`（＝原本的設計）是今天測過所有組合裡最好的。
結論是「本來那個就對」。⛔ 不要再調 head。

### ⇒ 真正的天花板不在 head

★ 就算餵給 head 的是**完全正確**的 `e_target`，誤差也只降 **約 9%**
（`0.2421` vs 只cond `0.2662`）。⇒ head 已經不是瓶頸，
**瓶頸是 `e_target` 比 `cond` 多知道的東西就只值那 9%**。

⏳ 下一步的方向應該回到 e_target / u 的設計本身（跟本檔開頭那個「u 沒有貢獻」的
發現接得起來），⛔ 不是繼續調 head。

### GCBC 0.15 vs 論文 99：⛔ 排除「success 判準不同」

`[實測]` 讀官方 `impls/utils/evaluation.py`：它的迴圈跑到 episode 結束才 `add_to(stats, flatten(info))`
—— 讀的是**最後一步**的 info；而 LaCoT 這邊是「碰到就算成功、立刻 break」。
看起來官方比較嚴（走到終點又飄走不算）。

⛔ **但不是這個。** `ogbench/locomaze/maze.py:42` 的 `terminate_at_goal=True` 是預設值，
success 當下就 `terminated = True` ⇒ 最後一步的 info 必定帶著 `success=1.0`。
⇒ **兩邊判準等價。**

⇒ 所以差異在 policy 本身。同 env、同資料，官方 GCBC 0.15、LaCoT 的 null-u BC 0.85。

⏳ 下一個要驗的（最便宜、最可能）：**動作分塊**。
pointmaze 的 observation 只有 (x,y)、沒有速度，而 point 有慣性 ⇒ 對只看當下位置的
Markovian policy 而言是 POMDP。官方**每步**重新決策；LaCoT **一次輸出 CHUNK=4 步**再執行
＝時間上的平滑，在慣性系統上反而穩。
⇒ 把 LaCoT 自己的 BC 的 `CHUNK` 從 4 改成 1，其他全不動：
- 掉到 0.15 附近 ⇒ 找到了，而且「分塊救了 POMDP 上的 BC」本身是有價值的發現
- 仍是 0.8 ⇒ 不是分塊，改查 JAX 版本那條
⛔ 不需要 JAX、不需要跑官方那套。

## 🚨 rollout 重跑：0.85 重現不出來，而配置沒留下來

`[實測]` 2026-08-23 傍晚，用 `scratch_m4_rollout.py`（已把 K / COND / CHUNK / STEPS2 /
EVAL_RS 全部改成環境變數）跑 `self, K=4, cond=256, chunk=4, 2000 步`，三個 seed：

```
                      null_u    R0     R3     R8
self K4 c256 ch4      0.417  0.570  0.547  0.523
  (seed 標準差)         0.157  0.121  0.133  0.134
ema  K4 c64  ch4      0.253  0.520  0.530  0.477
  (seed 標準差)         0.232  0.144  0.107  0.117
```

⛔ **本檔 §0 那組 0.85 重現不出來。** 原因不是隨機性 —— 是**配置沒有被記下來**：
- git 上兩個 commit 的 `K` 都是 **64**，而 §0 標的是 **K=4** ⇒ 昨晚手改了檔案、沒留。
- 腳本預設 `STEPS2=2000`，而本檔別處提到的是 **6000 步** ⇒ 步數同樣是手改的。

★ 已修：K / COND / CHUNK / STEPS2 / EVAL_RS 全部走環境變數，⛔ 不再靠手改檔案；
   輸出檔名帶上全部會變的設定（`rollout_{cons}_K{k}_c{cond}_ch{chunk}_st{steps}_s{seed}.json`）。
⚠️ 這一條踩了兩次：第一次漏了 chunk，第二次漏了 steps，而且第二次是 job 已經送出去
   才改腳本 ⇒ 各 job 讀到哪一版看它何時啟動。⇒ **改腳本前先確認沒有 job 在飛。**

### 觀察一：seed 噪聲大到蓋過一切

單格 seed 標準差 **0.11~0.23**。⇒ 3 個 seed × 100 集**撐不出 0.05 級別的結論**。
⛔ 以後談 rollout 上的差距，先看這個數字。

### 觀察二：u 有沒有貢獻，取決於 cond 這條捷徑有多寬

⭐ 同一個 seed ＝ 同一個訓練好的模型，只是把 u 開關 ⇒ 要做**配對**比較：

```
        seed0   seed1   seed2   平均
self    +0.190  -0.170  +0.130  +0.050   ← 有正有負，u 沒有穩定貢獻
ema64   +0.510  +0.310  +0.010  +0.277   ← 三個同號，u 真的在出力
```

cond 縮到 64 之後，只靠 cond 的成績掉到 0.11 / 0.07 ⇒ **捷徑被弄窄，u 才有事做**。
跟 §0-b「有 cond ⇒ u 可有可無；沒 cond ⇒ u 必要」是同一件事，只是這次是連續的。
⚠️ 但 ema 那組同時動了兩件事（cond 256→64、self→ema）⇒ ⛔ 分不出是誰造成的。

### 觀察三：沒有 test-time scaling

R0 / R1 / R3 / R5 / R8 全部落在同一個區間，多 refine 幾輪不會更好。

## 🚨🚨 「null-u floor」不是 BC 地板 —— 它是一個 OOD 探針

`[實測]` 主人 2026-08-23 問「為什麼 BC 愈練愈掉」，去翻 `scratch_m4_rollout.py` 的訓練迴圈：

```
total = l_nf + l_anchor + l_refine + 0.5 * l_cons
        flow  真et    refine後的u   一致性
```

⛔ **沒有任何一項是「u 餵 0」。** ⇒ head 在訓練時**從來沒看過 u 是零**。
⇒ eval 時把 u 塞成零去量「地板」＝餵它一個分布外的輸入。

⇒ 「愈練愈掉」解釋得通了：練愈久，head 對真實 u 的分布愈特化，零向量離它愈遠。
```
步數     null_u（OOD 探針）   LaCoT R3
 2000        0.417            0.547
 6000        0.500            0.733
12000        0.263            0.873
```
⛔ 掉的不是 BC 能力，是**分布外的懲罰**。

### 這條污染了哪些結論

⛔ **「配對差 +0.610 ⇒ u 的貢獻隨訓練長出來」要收回一半。** 那 0.61 混了兩件事：
u 真的帶了資訊、以及零向量是 OOD。⛔ 目前分不出各佔多少。

⚠️ 同一個問題也在本檔 §0：那組 `null-u floor ≈ LaCoT` 的結論同樣是用 `head(cond, 0)` 量的。
⇒ ⛔ 「u 沒有貢獻」與「u 貢獻很大」**兩個方向的結論都建立在同一個壞探針上**。

✅ **沒有受影響**：`R3` 那條線本身（0.547 → 0.733 → 0.873）—— 那是同一個度量在不同訓練長度
下的比較，LaCoT 確實隨訓練變好。主人「是時間問題」的判斷成立。

⇒ 修法：另外養一顆**從頭到尾只吃 cond、永遠不給 u** 的 head（＝`exp_three_fronts.run_head`
裡 `head_c` 的做法）。那才是誠實的 BC 地板。
★ 注意 `exp_three_fronts` 的 `run_train` **有** ZERO 分支、`run_head` **有** 獨立 head_c，
⛔ 只有 `scratch_m4_rollout.py` 兩者皆無 —— 跨腳本沿用「地板」這個詞是這次出錯的根。

## ✅ 誠實 BC 地板：u 沒有貢獻（結論回到 §0，但證據硬得多）

`[實測]` 加了一顆**獨立、只吃 cond、cond 已 `detach()`** 的 `bc_head`（跟 `ahead` 同容量、
同 optimizer；detach 是為了⛔ 不讓它的梯度回頭改造共用的 cond 編碼器 —— 否則
① 主模型被這個 baseline 改動、② 比較會系統性偏向「u 沒必要」）。

```
步數    誠實地板  OOD探針  LaCoT R3
 2000    0.607    0.420    0.553
 6000    0.723    0.487    0.733
12000    0.900    0.220    0.813
```

配對（同 seed 同訓練，只換讀哪顆 head）：`R3 - 地板` ＝ **-0.017 / +0.010 / -0.087**
⇒ **LaCoT 跟純 BC 打平，12000 步時還略輸。u 沒有貢獻。**

⛔ **本檔上一節那個「配對差 +0.610 ⇒ u 的貢獻隨訓練長出來」完全作廢** —— 它整個是
OOD 探針造成的假象。真地板 0.900、探針 0.220，中間那 0.68 全是「head 沒看過零」的懲罰。

### ⭐ 但形狀很怪，而這才是 LaCoT 真正的病

```
配對：地板 減 OOD探針 ＝ 「把 u 抽掉的傷害」
 2000   0.57→0.26  0.52→0.26  0.73→0.60   +0.233
 6000   0.61→0.04  0.79→0.74  0.77→0.68   +0.237
12000   0.74→0.07  0.97→0.05  0.99→0.54   +0.680
```

- u **給不給** → 沒有差（LaCoT 跟地板打平）
- u **抽掉**   → 全毀（0.97 → 0.05），而且傷害隨訓練變大

⇒ head **重度依賴 u，卻沒有從 u 得到任何好處**。它不是把 u 當資訊用，是把 u 當成一個
**必須在場的東西**，然後圍著它蓋了整套計算。⇒ **思考的內容沒被用上，思考的形式卻已被綁進去。**

⏳ 待驗（已送出）：**「別人的 u」探針** —— 從資料集隨機抽另一組 (s,g)，用它的 cond 生成
並 refine 出 u，再配上**本題**的 cond 餵給 head。分布完全正確、只有內容錯 ⇒ ⛔ 不製造 OOD。
- 成績不掉 ⇒ u 的內容根本沒被讀，head 只需要「那個位置有東西」
- 成績掉了 ⇒ 內容有被讀，只是沒比 cond 多帶東西

### 🎉 順帶解決了 GCBC 那條線

**LaCoT 自己的 BC 地板在 12000 步拿到 0.900，個別 seed 是 0.97 / 0.99。**
論文的 GCBC 是 99±6 ⇒ **對得上**。
⇒ 官方那份 code 跑出 0.15 **不是任務難、不是我們的 eval 有問題**，是那份 code 在這台機器上有毛病。
⛔ 這也表示「先跑官方 GCBC 當地基」這條路可以先擱著 —— 我們已經有一個到得了論文水準的地基。

⚠️ 保留：`clip_grad_norm_` 的清單裡也放了 `bc_head`，⇒ 這批的等效學習率跟先前幾批不同。
⛔ 跨批絕對值不可比；表內配對比較有效（同一批、同一個裁剪）。

## 🚨 為什麼一整天都證明不了 u 有用：選錯任務了

`[實測 一手來源]` 2026-08-23 親手翻 OGBench 論文 HTML（arXiv:2410.20092v2）Table 2：

```
任務                  GCBC   最強(QRL)   到最強   到滿分
pointmaze-medium-navigate   99      82        —       1
pointmaze-large-navigate    29      86       57      71
pointmaze-medium-stitch     23      80       57      77
pointmaze-large-stitch       7      84       77      93
```

🚨 **`medium-navigate` 是整個 benchmark 裡唯一一個 GCBC 打贏所有 RL 方法的任務**（99 vs QRL 82）。
⇒ 本檔前面所有「u 有沒有用」的實驗都跑在這一格上 —— 等於**在 BC 已經是 SOTA 的地方，
要證明別的東西更好**。LaCoT 的誠實地板量到 0.900、天花板 1.000，只剩 0.1 空間，
而 seed 噪聲就有 0.1 ⇒ ⛔ 這個任務**在資訊上就量不出 u 的價值**，不管 u 好不好。

★ 這條解釋了一整天的「打平」：不是方法沒效，是**尺規沒有刻度**。

⚠️ 查證過程本身也是教訓：先用搜尋，拿到的是「GCBC 14~20」，看起來剛好跟我們重現的
0.15 吻合、非常誘人。回去翻原文才發現那是**別人論文**引用 OGBench 時自己重跑的數字。
⇒ ⛔ 二手數字剛好支持你的假設時，最需要回一手來源。

### ⇒ 官方 GCBC 那條線的現況

```
論文寫的（medium-navigate）    99
官方 code 在這台跑             0.15   ← 仍未解釋
LaCoT 自己的 BC 地板           0.90   ← 跟論文對得上
```
⇒ ⛔ 壞的是官方那份 code 在這台機器上，不是任務、不是我們的 eval、不是分塊
（chunk=1 的 BC 是 0.387，沒有掉到 0.15）。
✅ 而我們已經有一個到得了論文水準的地基 ⇒ 「先跑官方 GCBC 當地基」這條可以擱著。

⏳ 已送出：`large-navigate` / `medium-stitch` / `large-stitch` 各 3 seed、12000 步、四探針全量。
先確認我們的 BC 也複製得出論文那個「BC 在這裡會爛」（29 / 23 / 7），地基才算對。

### 工具side：兩個安靜的坑

1. `ogbench.make_env_and_datasets` **不看** `OGBENCH_DATA_DIR`，只看 `dataset_dir` 參數
   （預設 `~/.ogbench/data`）⇒ 不傳它就**重新下載到 NFS home**（實測掉了 85M 進去）。已改成明確傳本機路徑。
2. Slurm 的 `scontrol update TimeLimit` 被這座叢集夾在 60 分（訊息寫 "interactive"），
   ⛔ 跑到一半才發現不夠是救不回來的；但**新提交**要 2~3 小時是准的 ⇒ 一開始就估好。

## 🚨🚨 通路斷在 refine：u 已經塌成一個常數

`[實測]` `experiments/u_variability.py`（載 checkpoint 直接量，⛔ 不跑環境、幾秒鐘）：

```
checkpoint                    |u|    逐維std  兩兩cos  refine前的cos
large-navigate  s0          0.1913  0.00028  1.0000     0.1057
large-navigate  s1          0.2100  0.00029  1.0000     0.1494
large-navigate  s2          0.2151  0.00053  1.0000     0.1677
medium-navigate s1          0.2353  0.00021  1.0000     0.1029
medium-nav 24000步 s0       0.1480  0.00008  1.0000     0.0984
medium-nav 24000步 s2       0.0865  0.00004  1.0000     0.0612
large-stitch    s0          0.3030  0.12614  0.9485     0.0568
medium-stitch   s1          0.3932  0.14263  0.9367     0.2879
```

🚨 **兩兩 cos ≈ 1.0000** ⇒ 所有題目的 `u` 是**同一個向量**。逐維標準差 `0.0002` 對量值 `0.235`
—— 差三個數量級。⇒ ⛔ **不是「head 不讀 u 的內容」，是根本沒有內容可讀。u 是一個常數。**

★ 最後一欄指出兇手：**refine 之前**的 u 兩兩 cos 只有 `0.06~0.34` ⇒ **flow 產生的 u 是有差異的**，
是 `refine` 把它們全部壓到同一點。⇒ **資訊在 flow 那端還在，走過 refine 就沒了。**

⇒ 這也回頭解釋了所有探針的結果：
- `shuffle`（別人的 u）＝ 真 u ⇒ 因為它們**本來就是同一個向量**
- `matched`（配對高斯）≈ 真 u ⇒ 因為 std≈0，matched 幾乎就是那個常數本身
- `gauss`（標準常態）與 `zero` 崩掉 ⇒ 都離那個常數很遠
- 拿掉 u 全毀、給 u 沒加分 ⇒ u 是 head 依賴的一個**學到的偏置**

### ⇒ 解已經在手上（早上量的），只是還沒接上

今早 anti-collapse 掃 11 個變體時，rollout 用的這個 `self consistency`
（`(u_r - u_{r+1}.detach())²`）正是會塌的那一種；**EMA teacher 的塌度 0.0564、三項全過**。
⛔ 但那批只跑 2000 步、也沒有量成功率 ⇒ 兩條線一直沒有接起來。

⏳ 下一步（三件同時到位才第一次有機會看到 u 在工作）：
**EMA teacher ＋ 12000 步 ＋ `large-stitch`**（BC 7 分、QRL 84 分，有 77 分空間的那個）。

★ 工具收穫：`scratch_m4_rollout.py` 現在會存 checkpoint ⇒ 換探針不必重訓
（今天為了換探針重訓了四輪，全部可以省掉）。`probe_u.py` / `u_variability.py` 都是吃 checkpoint 的。
