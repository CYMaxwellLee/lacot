# FINDINGS 2026-09-03 — 方言分辨日

出處分層：[實測] 親手量到　[主人] 主人親口　[推論] 從料推、沒直接驗

---

## ① 過夜替代臂收表（9/2 夜灑、9/3 早收；全部 [實測]）

收表：`experiments/collect_0903/collect_overnight_alts.py`（repo 的 collect_overnight.py 指 hard 判負前的舊臂，別用）。

```
                    平均    sd   爛顆   目錄
V8 基準 (s20~27)   .665  .149    0
soft 綁定          .752  .168    0    night_0902/dstart_soft
E' 新seed base     .663  .159    0    night_0902/heldout2_base   ← 散佈複製
E' 新seed soft     .744  .122    0    night_0902/heldout2_soft   ← 療效複製 +.080
HG 守門(V8 ckpt)   .926  .093    0    night_0902/guard_HG
soft＋守門(C')     .932  .063    0    night_0902/dssoft_HG
HGS 守門+吸附      .931  .026    0    night_0902/guard_HGS（n=4）
DA 純錨定          .307  .123    4    night_0902/guard_DA        ← 八顆確認判負
F' medium 官方     .794  .023    0    night_0902/medium_base     ← sd 超小
```

- E' 第二組獨立 seed（s28~35）把散佈（.663/.159 ≈ V8 .665/.149）與 soft 療效（+.080，
  第一組 +.087）都複製了 ⇒ 病與藥都不是 s20~27 的巧合。重抽籤也複製（s29 .796→.580、s28 .540→.984）。
- **medium 官方八顆 sd .023** ⇒ 方言病幾乎是 large 難度專屬（新發現，paper 表可用：.794±.023）。
- HG/HGS/C' 是安全網數字（開火集走 E 圖搜索），⛔ 別冒充 latent 計畫的數字。
- DA 八顆 .307 爛顆 4 ⇒ 9/2 夜四顆判負確認：推論時平移救不了。

## ② n_headguard 有計數沒出口 ⇒ 修好，並拿到開火率（[實測]）

- 9/2 批的守門開火數撈不到：`SUB_DIAG["n_headguard"]` 有計數，但輸出掛在 subgoal_diag
  的 `dsub/d0` gate 裡，conf2 沒收樣本時整段跳過 ⇒ 白計。log 的 🔎 行要 TRACE 才印。
- 修法（兩個 commit、smoke ALL PASS）：json 頂層直接寫 `n_headguard`（黏住後 replan 總數）
  ＋ `n_headguard_ep`（觸發黏住的集數 = 開火率分子；原計數當開火率讀會膨脹 ~10×）。
- **s20（soft、往返尺最好顆）開火率：133/250 = 53%**（rate .996；results/night_0903/hgcount2）。
  ⇒ soft 綁定治好資料端往返（mse .019），但 **flow 自己生的計畫、過半的集頭仍離現在 > 3**。
  生成端的頭沒被治，這是 C' (+.180) 的守門在幹的活、也是字彙錨定要打的殘餘。
- ⚠️ 單顆數字。八顆開火率要 eval 時全開 HG 才有（json 現在都會帶 n_headguard_ep）。

## ③ 方言分辨實驗：散佈住在 u 空間形狀裡（[實測]；主人 9/2 23:25 核）

凍 s20 soft stage 1（`LACOT_S1_FROM`）、stage 2 換 seed s40~47，其餘 V8 配方。
批腳本 `slurm/night_0903_dialect.sh`；收表 `experiments/collect_0903/collect_dialect.py`。

```
s40 .804  s41 .708  s42 .820  s43 .768  s44 .816  s45 .816  s46 .780  s47 .828
平均 .792  sd .040  範圍 .708~.828  爛顆 0
```

- **sd .168 → .040**（貼雜訊底；比 D 批「只換資料順序」的 .056 還小）。
- ⇒ **判決：抽籤（方言）幾乎全住在 stage 1 的 init（u 空間形狀）；stage 2 flow 對自己的 init 穩。**
  9/2 夜「stage 1 一動 stage 2 重洗牌」的機制修正：洗牌根源是 u 空間變了，不是 stage 2 自己抽籤。
- ⇒ 第一層解法「語言只教一次」（stage 1 訓一次、往返尺在 val 挑、凍住）＝這個實驗本身，
  免費驗證：.792±.040、零爛顆。s20 是往返尺挑的（.019），沒碰 test。
- ⇒ 按 9/2 夜的框架：字彙錨定要走就走 **FSQ**（格子固定、字彙跟 seed 無關）；
  「VQ＋分類治 stage 2 題型」的動機被削弱（stage 2 本來就穩）。
- ⏳ 第③步（進配方 vs FSQ vs 都排）等主人一起挑。
- ⚠️ 殘餘誠實面：單一 stage 1 的 .792 對 QRL .84 仍差；「挑哪顆 stage 1」本身仍是一次抽
  （往返尺是軟尺 r −.68）；可用 s26/s27 各凍一次驗「挑得好的 stage 1 都穩」（未跑）。

## ④ 側線（[實測]）

- **rapunzel GPU0 第三次掉**（CST 9/3 01:20、Xid 同款 bus 掉；haowei/marl2026 各一支 FAILED、
  GPU1 無事）；健康檢查 01:30 自動 drain（第三次生效）。主人問「換 PCIe 槽」⇒ 答：對調＝
  分辨實驗（跟卡走＝卡、留槽＝槽/線）；3090 電源兩條獨立 8-pin。⏳ 等主人動手，重開後驗收。
- 模型基底：9/3 起退回 claude-fable-5[1m]（營運凌晨改、主人指示；start-luna.sh 沿革註解）。
