# FINDINGS 2026-09-04 — 夜批判決＋fleet-command 首戰＋intent 三接法定案

## ① 夜批 N3/N4/N5 判決（早、80 支全收零失敗）

- **N3**：壓縮免費、round 付全費 — 8d 連續 z .842±.108（≈基線 .792+）；z甲 −.08 全是
  round 稅。⭐ R0 .456 ≈ z甲 .455 ⇒ 可讀性紅利壓縮就拿到、不用離散。
- **N4**：cond-drop 加壓到頂 — cd0.3 R0 .509（對 cd0.1 .505 平）、cd0.5 掉 .445
  ⇒ 字典線 R0 天花板 ~.51。
- **N5**：「挑得好的 stage 1 都穩」成立 — s26 .775±.086、s27 .857±.032（vs soft 原 .168）；
  s27 當日王座。品質差存在（.775~.857）⇒ 不是任挑皆可、是挑好的都穩。

## ② intent 三接法（主人裁「先 A 再 B」＋「categorial 加 continuous」；(iii) 主人提）

底盤：lacot/intent.py（hindsight／E 圖 BFS 選路／弧長重採樣 [T_A=32,2] float32 契約）；
nf_head Flow 3D cond（per-token prefix、Permutation flip 同步、golden 鎖 2D bit 級不變）；
主檔 LACOT_INTENT=embed|anchor|residual＋LACOT_INTENT_SRC=hindsight|route（A~K 十一項接線）。

**先導（單顆 s40、只判方向）**：ITA .976／ITE .944 R0 .524／ITE0 .960／residual 兩錨源亡。
**放量（各 8 顆）＝定案，先導兩格被翻**：

```
              subgoal        R0淨      判決
ITE  embed    .928±.075 ⭐   +.407 ⭐   雙腿新王座（vs N3 +.086、vs s27 +.071）
ITA  anchor   .881±.062     +.332     per-token 硬錨無加值（先導單顆 .976＝樂觀抽樣）
ITE0 無FSQ    .785±.158     +.201     「不依賴FSQ」被否決（一顆 .456 爛）；FSQ 仍地基
residual      判死          ~0        hindsight＝殘差無資訊 u 失業（health gate 攔）；
                                      route＝.412。⭐ 蹺蹺板：錨越準連續層越失業
```

⇒ **A 階段接口答案：查路線 → 壓全域摘要 → 進 cond（軟）**。比 per-token 耦合簡單且強。
⇒ 誠實格：ITE .928 vs ebfs ~.99 — subgoal 腿 u 仍輸純 BFS .06；u 的價值＝R0 腿＋高維可攜。

## ③ fleet-command 首戰（skill 同日誕生、主人發起＋Rei 心得雙向合流）

- 鏈：3 sonnet 模組 → opus 驗收官 8 發現（含攻擊實驗：換錯實作自測仍全過 ⇒ 弧長測試
  空轉實錘）→ sonnet 修 8 條 → opus 施工 A-K（J-1：273 tensors 逐位元同＝零行為差鐵證）
  → ルナ抽驗六 smoke 全綠。
- 三本帳實測：使魔側 ~82 萬 token（170+ 工具呼叫的 churn 全在牠們 context）；
  ルナ側只進六份收斂報告＋抽驗。
- 教訓入 insight store×3＋skill：測試先驗「壞時會叫」（zero-init 恆等假綠燈）；
  使魔回報＝二手貨物；ルナ context 留思考層。

## ④ 主人 Q&A（B 階段預備、rebuttal 素材 — 詳 DESIGN-2026-09-04 §兩問）

BFS 公平性（claim 擺法）／高維無 BFS（B＝字典搜索）／u vs BFS（表示≠搜索、腦手比喻、
u 已「取代搜索」試了半月＝.79-.86 vs .99）／退役四步（VQ 字典→資料學圖→token 空間
搜索→今天的接法接下層）。

## 殘留

- ⏳ ITA vs ebfs 的「u 有沒有差」直接對決表（數字都有、擺表即可）。
- ⏳ ITE0 R0 的 null_u > R0 怪格（單顆 .356 vs .204）— 放量後 net +.201 正常化，不追。
- ⏳ B 階段設計（字典化）等主人開題。
