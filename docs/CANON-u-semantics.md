# CANON — u 的正名（⛔ 不准漂）

_2026-09-06 主人裁示（TG 5699→5701）：早上到中午的抽象化討論把 u 的地基講丟了（「u 的意義已經不見得代表動作規劃了」）→ ルナ重建 → 主人核可並下令「記錄好，別再漂掉了」。本檔為 canonical、任何理論檔引用 u 時第一句先錨地基再抽象。_

## 地基（內容層、永遠是第一句）

**u = flow 生成的 latent 序列、編碼一段想像中的軌跡**（每段對應軌跡的一塊 chunk）。decode 出來就是一條具體路徑。u 代表動作規劃。

實測錨：可讀性探針終點 R²=.999（u 裡真有路線幾何）；decode 出的路徑可逐點對佔據圖查牆；shuf 腿（換別人的 u）R0=0 ⇒ u 內容 critical。

## 三問（抽象側面 — 都不取代地基）

1. **知識從哪來**：u 裡每一分知識都來自權重＋本題輸入 (s,g)；自生零資訊（CT-1）。⇒ u 是「規劃的顯影」、不是「規劃的來源」— 來源在牆（weights）。主人定調句「從 weights 提取肌肉經驗成 u」即此。
2. **為什麼有它比較好**：不加新資訊、加的是【計算結構】— 直接 (s,g)→整條動作序列是大跳；先攤成路線草稿（u）再讀動作＝兩個小跳（serial depth／frontier 疊加）。「工作檯」講的是這件事。
3. **品質誰決定**：u 的生成器是被教出來的 — 一代鷹架＝teacher（hindsight 軌跡、只教見過的）；二代鷹架（設計好未開工）＝verifier＋GRPO（裁判教、能教沒見過的）。⛔ u 本身不是鷹架 — 【教 u 的訊號】才是鷹架。

## u vs intent（一線之隔＝梯度繞不繞得過）

| | intent（小抄） | u（思考） |
|---|---|---|
| 來源 | 外來（hindsight 提示、掛 cond） | 自生（flow 生成） |
| 位置 | 可繞過（主幹夠用） | 繞不過（decoder 唯一輸入） |
| 命運 | 鷹架：在場紅利 +.14、內容零反應（swap 1.1%） | 承重：換別人的全砸（shuf=0） |

## 一句話（收攏版）

**u 是從牆裡攤出來的路線** — 內容是軌跡、知識來自權重、幫忙靠攤開、品質由老師決定。抽象名詞（context／工作檯／scratchpad）都是這條路線的側面、地基不動。

## ⛔ 漂移警示

抽象化討論（ICL／資訊帳本／info-vs-function／鷹架）一路走、每一步都對、但串起來會把「u=軌跡規劃」洗掉 — 主人 06:10 當場抓到。規則：**新檔或新討論引用 u、第一句先錨地基**（「u 編碼想像軌跡」）、再進抽象層。
